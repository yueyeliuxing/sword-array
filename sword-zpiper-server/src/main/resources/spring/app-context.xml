<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <context:component-scan base-package="com.dianwoba.user.*" />

    <dubbo:application name="${app.code}" logger="slf4j"/>

    <dubbo:protocol name="dubbo" port="${dubbo.protocol.port}" accesslog="true" />

    <!-- common 其他业务服务注册/消费zk -->
    <dubbo:registry id="commonDubboRegistry" address="zookeeper://${dubbo.common.zk}" />

    <!-- util 其他业务服务注册/消费zk -->
    <dubbo:routeServerRegistry id="unit" address="${rule.server.address}"/>

    <!-- gw 其他业务服务注册/消费zk -->
    <dubbo:registry id="dianwodaGwDubboRegistry" address="zookeeper://${dubbo.gw.zk}" />

    <!-- base 其他业务服务注册/消费zk -->
    <dubbo:registry id="dianwodaBaseDubboRegistry" address="zookeeper://${dubbo.base.zk}" />

    <!-- 默认注册zk -->
    <dubbo:provider registry="dianwodaGwDubboRegistry"  timeout="5000" />

    <!-- 默认消费zk -->
    <dubbo:consumer registry="dianwodaGwDubboRegistry" check="false" timeout="5000" />

</beans>